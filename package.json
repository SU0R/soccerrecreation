{
  "name": "algonquin-titans-soccer",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-deployable Python/Pygame soccer project for Vercel.",
  "scripts": {
    "build": "node scripts/build-game.mjs",
    "serve": "npx serve dist",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true});require('fs').rmSync('build',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
